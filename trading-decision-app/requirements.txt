# --- web server ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0

# --- HTTP client (dataflows: finnhub_pro / polygon_io / alpha_vantage / fmp) ---
requests>=2.31

# --- yfinance — 4th vendor in `fundamentals.py` fallback chain. Covers the
# metrics Finnhub free / Alpha Vantage drop (FCF, D/E, current ratio, ROIC,
# share count, etc.) — critical for banks and biotechs that other vendors
# miss. Also declared by TradingAgents' pyproject; listed here explicitly so
# the backend image always has it.
yfinance>=0.2.63

# --- translation layer (uses OpenAI-compatible API for any of:
#     OpenAI / DeepSeek / Qwen / GLM / Kimi / Google Gemini) ---
openai>=1.40

# --- Anthropic Claude (used by comprehensive_report when ANTHROPIC_API_KEY is set) ---
anthropic>=0.40

# --- optional: Supabase JWT verification ---
PyJWT>=2.8
cryptography>=42

# --- TradingAgents (LIVE mode) lives in ../TradingAgents/requirements.txt;
# install separately when you want LIVE mode.
